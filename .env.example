# Frontend -> Backend API base (for local dev)
VITE_API_URL=http://localhost:3001/api

# ============================================================================
# AI Provider API Keys (Front-end, injected at build time)
# ----------------------------------------------------------------------------
# IMPORTANT:
# - Keys must be set with VITE_ prefix for the frontend to see them.
# - Set these in Railway (or your CI) so Vite injects them during build.
# - Do NOT commit real keys.
# ----------------------------------------------------------------------------
VITE_GROQ_API_KEY=
VITE_TOGETHER_API_KEY=
VITE_GEMINI_API_KEY=
VITE_HF_API_KEY=

# Optional model overrides
# VITE_GROQ_MODEL=llama-3.3-70b-versatile
# VITE_TOGETHER_MODEL=meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo
# VITE_GEMINI_MODEL=gemini-2.0-flash-exp
# VITE_HF_MODEL=meta-llama/Llama-3.2-3B-Instruct

# PostgreSQL (Railway)
# One of these will be provided by Railway env
DATABASE_URL=postgresql://user:password@host:port/dbname
POSTGRES_URL=${DATABASE_URL}

# Email Notification Configuration
# Admin email address (receives login and chat notifications)
EMAIL_ADMIN_ADDRESS=admin@roofer.com

# Email sender address (from address)
EMAIL_FROM_ADDRESS=s21-assistant@roofer.com

# Email Provider Options (choose one):

# Option 1: SendGrid (recommended for production)
# Get API key from: https://sendgrid.com/
# SENDGRID_API_KEY=your_sendgrid_api_key_here

# Option 2: Resend (modern alternative)
# Get API key from: https://resend.com/
# RESEND_API_KEY=your_resend_api_key_here

# Option 3: Nodemailer with Gmail/SMTP
# For Gmail: Use app-specific password (not your regular password)
# Guide: https://support.google.com/accounts/answer/185833
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password

# Client-side email notifications toggle (optional, defaults to true)
# VITE_EMAIL_NOTIFICATIONS_ENABLED=true

# Admin email (client-side hint for showing Admin Panel)
# Used to grant admin role on login UI even if DB is not yet seeded
# VITE_ADMIN_EMAIL=admin@yourcompany.com

# Interactive Hail Maps (Enterprise)
IHM_API_KEY=
IHM_API_SECRET=
IHM_BASE_URL=https://maps.interactivehailmaps.com
