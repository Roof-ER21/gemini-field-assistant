# Frontend -> Backend API base (for local dev)
VITE_API_URL=http://localhost:3001/api

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# PostgreSQL (Railway)
# One of these will be provided by Railway env
DATABASE_URL=postgresql://user:password@host:port/dbname
POSTGRES_URL=${DATABASE_URL}

# Email Notification Configuration
# Admin email address (receives login and chat notifications)
EMAIL_ADMIN_ADDRESS=admin@roofer.com

# Email sender address (from address)
EMAIL_FROM_ADDRESS=s21-assistant@roofer.com

# Email Provider Options (choose one):

# Option 1: SendGrid (recommended for production)
# Get API key from: https://sendgrid.com/
# SENDGRID_API_KEY=your_sendgrid_api_key_here

# Option 2: Resend (modern alternative)
# Get API key from: https://resend.com/
# RESEND_API_KEY=your_resend_api_key_here

# Option 3: Nodemailer with Gmail/SMTP
# For Gmail: Use app-specific password (not your regular password)
# Guide: https://support.google.com/accounts/answer/185833
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password

# Client-side email notifications toggle (optional, defaults to true)
# VITE_EMAIL_NOTIFICATIONS_ENABLED=true
